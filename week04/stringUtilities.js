export const capitaliseWord = (word) =>
    `${word.charAt(0).toUpperCase()}${word.slice(1)}`;

export const lowerCaseWord = (word) => word.toLowerCase();

export const upperCaseWord = (word) => word.toUpperCase();
